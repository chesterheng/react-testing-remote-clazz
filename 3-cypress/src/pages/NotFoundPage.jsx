import React, { Fragment } from 'react';
import { PAGE_NOT_FOUND } from '../constants/labelTypes';

const NotFoundPage = () => {
  return (
    <Fragment>
      {PAGE_NOT_FOUND}
    </Fragment>
  );
};

export default NotFoundPage;